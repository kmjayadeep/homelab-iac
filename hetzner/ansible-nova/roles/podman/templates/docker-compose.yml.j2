version: '3.8'

services:
  # Simple nginx app container (placeholder)
  nginx-app:
    image: docker.io/nginx:1.25-alpine
    container_name: nginx-app
    restart: always
    ports:
      - "8080:80"
    networks:
      - nova-network

  # Example service - uncomment and customize as needed  
  # my-app:
  #   image: docker.io/my-app:latest
  #   container_name: my-app
  #   restart: always
  #   ports:
  #     - "3000:3000"  # Bind to host for nginx access
  #   networks:
  #     - nova-network
  #   environment:
  #     - NODE_ENV=production
  
  # Database example - uncomment as needed  
  # postgres:
  #   image: docker.io/postgres:15-alpine
  #   container_name: postgres
  #   restart: always
  #   networks:
  #     - nova-network
  #   environment:
  #     - POSTGRES_DB=myapp
  #     - POSTGRES_USER=myuser
  #     - POSTGRES_PASSWORD=mypassword
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

networks:
  nova-network:
    driver: bridge

# Uncomment when you add services that need persistent storage
# volumes:
#   postgres_data:
