server {
  listen 80;
  listen [::]:80;
  server_name {{ kite_domain }};
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  server_name {{ kite_domain }};

  ssl_certificate /etc/letsencrypt/live/{{ kite_cert_domain }}/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/{{ kite_cert_domain }}/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;

  location / {
    proxy_pass http://{{ kite_upstream_host }}:{{ kite_upstream_port }};
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
  }
}
