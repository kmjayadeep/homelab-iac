---
- name: Restart Windrose Server
  hosts: windrose_servers
  become: true

  tasks:
    - name: Restart Windrose server with helper CLI
      ansible.builtin.command:
        cmd: ./windrose restart
        chdir: "{{ windrose_project_dir }}"
      become_user: "{{ windrose_user }}"
