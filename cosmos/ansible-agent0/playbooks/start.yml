---
- name: Start Agent0 service
  hosts: agent0_servers
  tasks:
    - name: Start Agent0
      ansible.builtin.systemd:
        name: "{{ agent0_service_name }}"
        state: started
        scope: user
        daemon_reload: true
      become_user: "{{ agent0_user }}"
